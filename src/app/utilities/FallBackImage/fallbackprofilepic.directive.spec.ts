import { FallbackprofilepicDirective } from './fallbackprofilepic.directive';

describe('FallbackprofilepicDirective', () => {
  it('should create an instance', () => {
    const directive = new FallbackprofilepicDirective();
    expect(directive).toBeTruthy();
  });
});
