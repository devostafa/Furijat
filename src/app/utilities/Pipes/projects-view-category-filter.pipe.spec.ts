import { ProjectsViewCategoryFilterPipe } from './projects-view-category-filter.pipe';

describe('ProjectsViewCategoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectsViewCategoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
